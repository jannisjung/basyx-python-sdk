include:
  - ./tests/integration/basyx_test_environment/docker-compose.yml

services:
  basyx-client:
    container_name: basyx-client
    build: .
    image: basyx-client:dev
    volumes:
      - .:/basyx_client
    ports:
      - "5001:5001"
      - "5000:5000"
